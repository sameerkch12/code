function OTPgenerator(limit)
{
    let digit= '0123456789';
    let otp = '';

    for(let i=0; i<limit; i++)
    {
        otp+= digit[Math.floor(Math.random() * 10)];
    }
    return otp;
}
    console.log(OTPgenerator(4)); //8188
    console.log(OTPgenerator(4)); //0019
